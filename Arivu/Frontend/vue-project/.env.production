# Frontend Production Configuration
# For Electron builds, the backend URL will be dynamically configured
# via the Electron IPC API (electronAPI.getApiConfig())
#
# If building for web deployment (non-Electron), set this to your backend URL
VITE_API_BASE_URL=http://localhost:8000
